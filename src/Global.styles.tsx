import { GlobalStyles } from '@mui/material';

const Styles = () => {
  return (
    <GlobalStyles
      styles={{
        body: {
          fontFamily: "'Nunito Sans'",
        },
      }}
    />
  );
};

export default Styles;
